apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: k8rest-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: k8rest-service
                port:
                  number: 8000
